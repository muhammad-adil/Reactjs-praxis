import React, { Component } from "react";

export default class Header extends Component {
  // super(props);
  render() {
    return <h2 className="App-intro">Hello from header.js in component</h2>;
  }
}
